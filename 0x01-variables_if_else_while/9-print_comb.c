#include <stdio.h>

/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{

	int n;

	for (n = 0; n < 10; n++)
	{
		putchar(a + '0');
		if (n < 9)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
